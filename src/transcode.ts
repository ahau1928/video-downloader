export interface TranscodeOptions {codec:string;mode:string;crf_h264:number;crf_h265:number;preset_h264:string;preset_h265:string;target_mb:number;audio:string;audio_kbps:number;resolution:string;width:number;height:number;lock_ratio:boolean;anchor:string;fit:string;suffix_mode:string;suffix:string;container:string;color:string}
export const defaultTranscode:TranscodeOptions={codec:'h264',mode:'crf',crf_h264:23,crf_h265:28,preset_h264:'medium',preset_h265:'medium',target_mb:100,audio:'encode',audio_kbps:192,resolution:'source',width:1920,height:1080,lock_ratio:true,anchor:'width',fit:'pad',suffix_mode:'auto',suffix:'',container:'mp4',color:'auto'};
export interface LocalMedia {path:string;duration:number;size:number;width:number;height:number;codec:string;fps:number;audio_codec:string|null;audio_kbps:number|null;hdr:boolean;depth:number}
export interface TranscodePlan {media:LocalMedia;width:number;height:number;audio_kbps:number;audio_mb:number;video_kbps:number|null;output_name:string;warning:string;pixel_format:string}
